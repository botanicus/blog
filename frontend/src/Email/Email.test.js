/* TODO: Make sure the obfuscation works before testing this. */
/* Or should I use contact form? */

// import React from 'react'
// import TestRenderer from 'react-test-renderer'
// import Email, { SelfLinkingEmail } from './Email'

// it('renders a <div> tag with class highlight and children inside', () => {
//   const testRenderer = TestRenderer.create(<Email subject="Hello Jakub">Lorem ipsum</Email>)
//   const object = testRenderer.toJSON()

//   console.log(object)
//   expect(object.type).toEqual('a')
//   // expect(object.props.className).toEqual('highlight')
//   expect(object.children).toEqual(['Lorem ipsum'])
// })

// it('fails if no children are provided', () => {
//   expect(() => TestRenderer.create(<Email />)).toThrow('Email requires children')
// })

it('needs one test', () => expect('fuck off').toEqual('fuck off'))
